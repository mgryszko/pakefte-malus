[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
stravalib = "==0.10.4"
fastapi = "==0.70.0"
uvicorn = "==0.15.0"
python-telegram-bot = "==13.8.1"
cryptography = "==35.0.0"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
test = "python -m unittest"
server = "uvicorn server:app --host=0.0.0.0 --port=8080 --reload"
